{
  "name": "liquefied",
  "version": "0.0.1",
  "description": "A monorepo for a message broker system in nodejs typescript",
  "author": "Aurelien Clave",
  "license": "MIT",
  "private":true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test",
    "build": "lerna run build",
    "release": "yarn test && yarn build && lerna publish"
  },
  
  "devDependencies": {
    "lerna": "^6.4.1",
    "@babel/preset-typescript": "^7.18.6",
    "@babel/preset-env":"^7.20.2",
    "@types/jest": "^29.2.5",
    "@types/node": "^18.11.18",
    "jest": "^29.3.1",
    "ts-jest": "^29.0.5",
    "typescript": "^4.9.4"
  },
  "workspaces": [
    "packages/*"
  ]
}
